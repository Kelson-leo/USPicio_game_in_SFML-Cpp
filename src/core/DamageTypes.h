#pragma once

namespace core {

enum class AttackType {
    Punch,
    Throw,
    EnemyTouch,
    BossProjectile
};

enum class EntityType {
    Player,
    Capivara,
    Professor
};

} // namespace core
